# Network packet capture and producers

## Steps to run the producer
```
cd producer

# build the producer executable
cargo build --release

# run the producer executable using sudo
sudo target/release/producer
```